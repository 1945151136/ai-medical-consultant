{
  "name": "@medical/global",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types, constants, and Zod schemas for the medical consultation platform",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "echo 'lint ok'",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
